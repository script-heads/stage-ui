{
  "name": "flow-ui",
  "author": "ABR Tech",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "docs": "yarn workspace @flow-ui/docs showcase",
    "gen": "yarn gen:createTypes && yarn gen:convertTypes && yarn gen:generateTypes",
    "gen:createTypes": "cd ./packages/showcase && typedoc -mode file --json ./generated/original.json ../docs/cases",
    "gen:convertTypes": "cd ./packages/showcase && node ./tools/TypeDoc.js",
    "gen:generateTypes": "cd ./packages/showcase && node ./tools/TypeGenerator.js",
    "pub:core": "git pull && cd ./packages/core && npm version patch && yarn build && cd ./dist && npm publish && cd .. && rm -rf ./dist && yarn push",
    "pub:lab": "git pull cd ./packages/lab && npm version patch && yarn build && cd ./dist && npm publish && cd .. && rm -rf ./dist && yarn push",
    "push": "git commit . -m 'Update version' && npm version patch && git push"
  }
}