import { css } from '@emotion/core'

export default {
    container: css({
        padding: '0.5rem',
        border: '2px dashed rgba(0,0,0,0.25)',
        borderRadius: '5px',
        margin: '0.75rem 0.5rem',
    }),
}