{
    "name": "@stage-ui/icons",
    "author": "Research technology",
    "version": "0.1.6",
    "license": "MIT",
    "scripts": {
        "pub": "git pull && yarn build && npm version patch && cd ../../build/icons && npm version patch && yarn publish --access public && cd .. && rm -rf ./icons",
        "build": "yarn tsc",
        "gen": "node ./scripts/generate"
    },
    "dependencies": {},
    "devDependencies": {},
    "peerDependencies": {
        "@stage-ui/system": "*",
        "@emotion/react": "*"
    }
}
