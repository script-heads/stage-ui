{
    "name": "@stage-ui/icons",
    "author": "Research technology",
    "version": "0.2.0",
    "license": "MIT",
    "scripts": {
        "pub": "git pull && npm version patch && yarn build && cd ../../build/icons && yarn publish --access public && cd .. && rm -rf ./icons",
        "build": "tsc && cp package.json README.md ../../build/icons/ && node ../../scripts/buildConfig icons",
        "gen": "node ./scripts/generate"
    },
    "dependencies": {},
    "devDependencies": {},
    "peerDependencies": {
        "@stage-ui/system": "*",
        "@emotion/react": "*"
    }
}
