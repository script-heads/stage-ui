import React, { Fragment } from 'react'
import { Display } from '@flow-ui/core'

export default () => {
    return (
        <Fragment>
            <Display>That's a big one</Display>
        </Fragment>
    )
}