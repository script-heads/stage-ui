import React from 'react'
import { Viewport } from '@flow-ui/core'

export default () => {
    return (
        <Viewport>
            Viewport provide theme
            to all Flow components
        </Viewport>
    )
}